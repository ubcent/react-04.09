import { handleActions } from 'redux-actions';

import {} from 'actions/comments';

const initialState = {
    entities: [],
    loading: true,
};

export default handleActions({

    },
    {initialState});